# EasyZapret

EasyZapret — небольшое приложение для Windows, которое помогает запускать и настраивать Zapret, Telegram Proxy и Cloudflare WARP без консоли и ручного редактирования `.bat`-файлов.

Проект не содержит Zapret внутри репозитория. Нужные компоненты скачиваются при первом запуске и хранятся в `C:\EasyZapret`.

## Скачать

Готовый установщик находится на [странице релизов](https://github.com/laciodanya/easyzapret/releases/latest). Откройте последний релиз и скачайте файл `EasyZapret_*_x64-setup.exe` из блока **Assets**.

Для работы нужны:

- Windows 10 или 11, x64;
- права администратора;
- интернет при первом запуске.

Windows SmartScreen может предупредить о неизвестном издателе, потому что установщик пока не подписан коммерческим сертификатом. В этом случае нажмите **Подробнее → Выполнить в любом случае**.

</br>

<img width="1089" height="710" alt="image" src="https://github.com/user-attachments/assets/9ce9531e-95d4-4a6e-872b-ac4f758e58c1" />

</br>

## Что умеет EasyZapret

- запускает Zapret с выбранной стратегией;
- устанавливает Zapret как службу Windows;
- сравнивает стратегии и помогает выбрать рабочую;
- редактирует пользовательские списки доменов и IP;
- запускает Telegram Proxy и создаёт ссылку для Telegram Desktop;
- управляет Cloudflare WARP и следит, чтобы Zapret запускался раньше него;
- может стартовать вместе с Windows и работать из трея;
- проверяет доступность сервисов в фоне через Автопилот;
- обновляет приложение и компоненты из интерфейса;
- показывает логи и базовую диагностику проблем.

## Первый запуск

После установки EasyZapret предложит скачать Zapret и Telegram Proxy. Все файлы будут лежать здесь:

```text
C:\EasyZapret\
├── zapret\
├── tg-ws-proxy\
├── logs\
├── tmp\
└── config.json
```

Дальше достаточно выбрать стратегию на странице **Стратегии** и включить Zapret на главном экране.

Если нужен WARP, сначала установите официальный клиент [Cloudflare WARP](https://one.one.one.one/). EasyZapret подключит его только после запуска Zapret.

## Важные моменты

### Антивирус и WinDivert

Zapret использует `winws.exe` и драйвер WinDivert. Некоторые антивирусы удаляют их по ошибке. Если Zapret перестал запускаться или пропал `WinDivert64.sys`, добавьте `C:\EasyZapret` в исключения и переустановите компонент в настройках.

### Защищённый DNS

Если часть сайтов открывается нестабильно, попробуйте включить DNS-over-HTTPS в Windows или браузере. Обычно подходят `1.1.1.1` и `8.8.8.8`.

### Telegram Proxy

Если через прокси не загружаются фото и видео, откройте настройки прокси в Telegram и удалите записи DC → IP, оставив только `4:149.154.167.220`. Если это не поможет, очистите список полностью.

## Сборка из исходников

Понадобятся Node.js, Rust и WebView2. Собирать установщик нужно на Windows.

```bash
npm install
npm run tauri dev
npm run tauri build
```

Frontend написан на React и TypeScript, backend — на Rust и Tauri 2. Установщик собирается через NSIS.

## Где хранятся данные

Настройки, пользовательские списки и логи не лежат в папке установки приложения. Они остаются в `C:\EasyZapret`, поэтому переустановка программы не удаляет ваши настройки.

## Благодарности

- [bol-van](https://github.com/bol-van) — оригинальный [zapret](https://github.com/bol-van/zapret) и winws;
- FlowSeal — сборки zapret-discord-youtube и tg-ws-proxy;
- [hyperion-cs/dpi-checkers](https://github.com/hyperion-cs/dpi-checkers) — набор DPI-чекеров.

EasyZapret — независимый пользовательский проект. Используйте его на свой риск и соблюдайте законы своей страны.
